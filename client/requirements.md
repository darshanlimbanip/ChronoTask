## Packages
framer-motion | Page transitions and smooth micro-interactions
recharts | Beautiful data visualization for the dashboard
date-fns | Robust date formatting and calculations
react-hook-form | Form state management
@hookform/resolvers | Zod validation integration for forms
clsx | Class name utility (if not already fully available)
tailwind-merge | Tailwind class merging (if not already fully available)

## Notes
Using standard Lucide React icons.
Using `@/components/ui` for standard Shadcn building blocks, enhanced with custom CSS elevation classes.
Using space-separated HSL for colors.
